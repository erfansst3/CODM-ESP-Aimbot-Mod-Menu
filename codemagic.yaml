workflows:
  android-ndk-debug:
    name: Android NDK Debug Build

    environment:
      java: 17
      ndk: r21d
    scripts:

      - name: Build Debug APK
        working_directory: android
        script: yes | ./gradlew assembleDebug

    artifacts:
      - app/build/outputs/**/*.apk
